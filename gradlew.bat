@echo off
where gradle >nul 2>nul
if %ERRORLEVEL% EQU 0 (
  gradle %*
  exit /b %ERRORLEVEL%
)
echo Gradle is not installed. Run the GitHub Actions workflow; it generates the official Gradle Wrapper.
exit /b 1
