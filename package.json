{
  "name": "ethos-nexum-app",
  "version": "1.0.0",
  "private": true,
  "description": "Éthos Nexum — Ideias que conectam.",
  "scripts": {
    "build": "node -e \"console.log('Site estático pronto em www/')\"",
    "cap:sync": "npx cap sync",
    "cap:copy": "npx cap copy",
    "cap:build:apk": "npx cap build android --androidreleasetype APK",
    "cap:build:aab": "npx cap build android --androidreleasetype AAB"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  }
}
