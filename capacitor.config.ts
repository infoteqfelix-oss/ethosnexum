import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ethosnexum.app',
  appName: 'Éthos Nexum',
  webDir: 'www',
  bundledWebRuntime: false,
  android: {
    backgroundColor: '#11100e'
  },
};

export default config;
